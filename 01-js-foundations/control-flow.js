// Control flow

// if / else if / else
// switch
// for, for...of (values), for...in (keys), while, do...while

for (const n of [1, 2, 3]) {
  if (n % 2 === 0) console.log(`${n} is even`);
  else console.log(`${n} is odd`);
}

// TODO: break/continue, labeled loops, switch fall-through
