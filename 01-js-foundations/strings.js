// Strings

const s = "  Hello World  ";

// Methods to know: length, trim, toUpperCase/toLowerCase, slice, substring,
// indexOf, includes, replace, split, concat, template literals
console.log(s.trim());                 // "Hello World"
console.log(s.trim().split(" "));      // ["Hello", "World"]
console.log(`len = ${s.trim().length}`); // template literal

// TODO: padStart/padEnd, repeat, startsWith/endsWith, regex replace
