// Objects — use named indexes (keys).

const empty = {};
const point = { x: 1, y: 2 };

// Access
console.log(point.x);      // dot notation
console.log(point["y"]);   // bracket notation

// TODO: shorthand properties, computed keys, Object.keys/values/entries,
//       spread copy, optional chaining (?.), nullish coalescing (??), `this`
