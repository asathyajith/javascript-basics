// DOM Events (browser environment)
//
// Common events: click, change, mouseover, mouseout, keydown, keyup, submit, load
//
// element.addEventListener("click", handler) is preferred over inline onclick.

// Example (runs in a browser, not Node):
// const btn = document.querySelector("#myBtn");
// btn.addEventListener("click", (event) => {
//   console.log("clicked", event.target);
// });

// TODO: event object, preventDefault, stopPropagation, event delegation, bubbling vs capturing
